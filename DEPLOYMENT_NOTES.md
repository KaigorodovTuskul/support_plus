# Заметки для деплоя на VPS

## Оптимизация для VPS

Проект был оптимизирован для легкого деплоя на VPS:

### ✅ Что было сделано:

1. **Убраны тяжелые зависимости**
   - ❌ `sentence-transformers` (~1.5 GB с PyTorch)
   - ❌ `torch` (~110 MB)
   - ❌ `transformers`
   - ❌ `faiss-cpu`
   - ❌ `openai` (не используется)

2. **Используются только облачные API**
   - ✅ Hyperbolic API для чат-бота (Llama-3.3-70B)
   - ✅ Hyperbolic API для категоризации документов
   - ✅ Mistral API для голосового ассистента (STT/TTS)
   - ✅ Все через простые HTTP запросы с `requests`

3. **Заглушки для локальных моделей**
   - `backend/search/embedding_service.py` - возвращает пустые embeddings
   - `backend/search/query_parser.py` - простой fallback парсинг

### 📦 Итоговый размер зависимостей

Вместо ~2 GB (с PyTorch и моделями), теперь **~50 MB** легковесных пакетов.

### 🚀 Быстрый старт на VPS

```bash
# 1. Клонировать репозиторий
git clone <repo-url>
cd hacktheice

# 2. Установить легковесные зависимости
cd backend
pip install -r requirements.txt  # ~50 MB вместо ~2 GB!

# 3. Настроить .env
echo "MISTRAL_API_KEY=your_key" >> .env
echo "OLLAMA_API_KEY=your_key" >> .env

# 4. Применить миграции
python manage.py migrate

# 5. Запустить сервер
python manage.py runserver 0.0.0.0:8000
```

### 🔑 Необходимые API ключи

В `.env` файле нужен только **один ключ**:

```env
MISTRAL_API_KEY=your_mistral_api_key
```

**Все функции работают через Mistral API:**
- Чат-бот: `mistral-large-latest`
- Категоризация CSV: `mistral-small-latest` (быстрее и дешевле)
- Голосовой ассистент: Mistral STT/TTS

### 📝 API Endpoints

**Чат-бот:**
- `POST /api/chat/` - отправить сообщение
- `GET /api/chat/history/` - история чата
- `DELETE /api/chat/clear/` - очистить историю

**Голосовой ассистент:**
- `POST /api/voice/stt/` - речь → текст (Mistral)
- `POST /api/voice/tts/` - текст → речь (Mistral)

### ⚠️ Важные заметки

1. **Векторный поиск отключен** - `LocalEmbeddingService` возвращает пустые embeddings
2. **OpenAI парсер отключен** - `QueryParser` использует простой fallback
3. Если нужен векторный поиск - используйте облачные решения:
   - [Pinecone](https://www.pinecone.io/)
   - [Weaviate Cloud](https://weaviate.io/)
   - [Qdrant Cloud](https://qdrant.io/)

### 🎯 Рекомендации для продакшена

1. Используйте Gunicorn вместо `runserver`:
   ```bash
   pip install gunicorn
   gunicorn config.wsgi:application --bind 0.0.0.0:8000
   ```

2. Настройте nginx как reverse proxy
3. Используйте PostgreSQL вместо SQLite
4. Настройте HTTPS с Let's Encrypt
5. Добавьте rate limiting для API endpoints

### 📊 Размер установки

| Компонент | С локальными моделями | Только API |
|-----------|----------------------|------------|
| PyTorch | ~110 MB | ❌ |
| sentence-transformers | ~1.5 GB | ❌ |
| Модели HuggingFace | ~1-2 GB | ❌ |
| Django + зависимости | ~50 MB | ✅ ~50 MB |
| **ИТОГО** | **~2.7 GB** | **~50 MB** |

**Экономия: ~98% дискового пространства и RAM!**

### ✅ Готово к деплою

Проект полностью готов к деплою на любом VPS с минимальными требованиями:
- 1 GB RAM (вместо 4+ GB)
- 1 GB диск (вместо 10+ GB)
- Python 3.11+
- Интернет для API запросов
