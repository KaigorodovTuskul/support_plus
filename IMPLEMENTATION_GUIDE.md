# Руководство по реализации

## Обзор выполненных задач

Все три задачи были выполнены:

1. ✅ Изучена структура команд manage.py для заполнения БД
2. ✅ Создан скрипт предобработки CSV с категоризацией через minimax-m2:cloud
3. ✅ Добавлен чат-бот с голосовым ассистентом

---

## Задача 1: Команды для заполнения БД

### Расположение
Команды находятся в `backend/search/management/commands/`:
- `populate_from_csv.py` - импорт льгот из CSV
- `rebuild_index.py` - перестроение поискового индекса

### Модель данных
- **Benefit** (`backend/benefits/models.py`) - основная модель льгот
- Поддерживает категории, регионы, целевые группы
- Связь many-to-many с Category и Region

---

## Задача 2: Предобработка CSV с категоризацией

### Файл: `preprocess_categories.py`

Этот скрипт:
1. Читает `sfr_invalidam_data.csv`
2. Использует **minimax-m2:cloud** через Ollama API для определения категории каждого документа
3. Сохраняет результат в `sfr_invalidam_data_categorized.csv`

### Категории
```python
CATEGORIES = [
    "Пенсионное обеспечение",
    "Технические средства реабилитации",
    "Медицинская помощь",
    "Социальные выплаты",
    "Образование",
    "Жилищные льготы",
    "Транспортные льготы",
    "Налоговые льготы",
    "Трудоустройство",
    "Юридическая помощь",
    "Другое"
]
```

### Запуск предобработки

```bash
# Убедитесь, что .env содержит OLLAMA_API_KEY
cd g:/hacktheice

# Установите зависимости
pip install ollama python-dotenv

# Запустите скрипт
python preprocess_categories.py
```

Скрипт создаст файл `sfr_invalidam_data_categorized.csv` с новой колонкой `category`.

### Обновленная команда populate_from_csv

Команда была обновлена для поддержки колонки категории:
- Если в CSV есть колонка `category`, создается/используется соответствующая категория
- Если колонки нет, используется категория по умолчанию "Льготы для инвалидов"

### Заполнение БД с категориями

```bash
cd backend

# Применить миграции
python manage.py makemigrations chatbot
python manage.py migrate

# Импорт с категориями
python manage.py populate_from_csv ../sfr_invalidam_data_categorized.csv --clear

# Или без категорий (используется CSV без предобработки)
python manage.py populate_from_csv ../sfr_invalidam_data.csv --clear
```

---

## Задача 3: Чат-бот с голосовым ассистентом

### Бэкенд

#### Новое приложение: `backend/chatbot/`

**Модели** (`models.py`):
- `ChatMessage` - хранит историю чата пользователя

**API эндпоинты** (`views.py`, `urls.py`):
- `POST /api/chat/` - отправка сообщения в чат-бот
- `GET /api/chat/history/` - получение истории чата
- `DELETE /api/chat/clear/` - очистка истории
- `POST /api/voice/stt/` - преобразование речи в текст (Speech-to-Text)
- `POST /api/voice/tts/` - преобразование текста в речь (Text-to-Speech)

**Интеграция с моделями**:
- **Чат-бот**: minimax-m2:cloud через Ollama API
- **Голосовой ассистент**: Mistral API для STT и TTS

#### Настройки

`.env` файл должен содержать:
```env
OLLAMA_API_KEY=bdb94e94de8141c181bcba756eb6ca37.m606XYMn7CcnWNwOunadbvlG
MISTRAL_API_KEY=aKiZcTiksAipZf2nsfTlsE1xOd5jlYtR
```

#### Добавлено в settings.py
```python
INSTALLED_APPS = [
    ...
    'chatbot'
]
```

#### Добавлено в urls.py
```python
urlpatterns = [
    ...
    path('api/', include('chatbot.urls')),
]
```

### Фронтенд

#### Компонент: `frontend/components/Chatbot.vue`

**Функции**:
- 💬 Текстовый чат с minimax-m2:cloud
- 🎤 Запись голосовых сообщений
- 🔊 Воспроизведение ответов голосом
- 📜 История чата (сохраняется в БД)
- 🗑️ Очистка истории
- ✨ Красивый UI с анимациями

**Интеграция**:
Компонент добавлен на страницу `/dashboard` - доступен только после авторизации.

**Использование голосового ввода**:
1. Нажмите кнопку микрофона
2. Говорите ваш вопрос
3. Нажмите "Остановить"
4. Речь автоматически преобразуется в текст через Mistral API
5. Отправьте сообщение как обычно

**Воспроизведение ответов**:
- Ответы от бота автоматически воспроизводятся голосом через Mistral TTS
- Можно отключить, если не нужно

---

## Установка и запуск

### 1. Установите зависимости

```bash
# Backend
cd backend
pip install -r requirements.txt

# Frontend
cd ../frontend
npm install
```

### 2. Настройте переменные окружения

Убедитесь, что `.env` содержит:
```env
MISTRAL_API_KEY=aKiZcTiksAipZf2nsfTlsE1xOd5jlYtR
OLLAMA_API_KEY=bdb94e94de8141c181bcba756eb6ca37.m606XYMn7CcnWNwOunadbvlG
```

### 3. Примените миграции

```bash
cd backend
python manage.py makemigrations chatbot
python manage.py migrate
```

### 4. (Опционально) Предобработайте CSV

```bash
cd ..
python preprocess_categories.py
```

### 5. Заполните БД

```bash
cd backend

# С категориями (если выполнили шаг 4)
python manage.py populate_from_csv ../sfr_invalidam_data_categorized.csv --clear

# Или без категорий
python manage.py populate_from_csv ../sfr_invalidam_data.csv --clear
```

### 6. Запустите проект

```bash
# Backend
cd backend
python manage.py runserver

# Frontend (в новом терминале)
cd frontend
npm run dev
```

### 7. Проверьте чат-бот

1. Откройте http://localhost:3000
2. Войдите в систему
3. Перейдите в Dashboard
4. В правом нижнем углу появится кнопка чата
5. Нажмите и начните общение!

---

## API примеры

### Отправка сообщения

```bash
curl -X POST http://localhost:8000/api/chat/ \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"message": "Какие льготы доступны для инвалидов 1 группы?"}'
```

### Получение истории

```bash
curl http://localhost:8000/api/chat/history/ \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Голосовой ввод (STT)

```bash
curl -X POST http://localhost:8000/api/voice/stt/ \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"audio": "BASE64_AUDIO_DATA"}'
```

### Синтез речи (TTS)

```bash
curl -X POST http://localhost:8000/api/voice/tts/ \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"text": "Здравствуйте! Я могу помочь вам с информацией о льготах."}'
```

---

## Примечания

### Категоризация документов
- Модель minimax-m2:cloud используется для автоматической категоризации
- Скрипт обрабатывает каждый документ и определяет наиболее подходящую категорию
- Категории хранятся в базе данных и связаны с льготами через many-to-many

### Чат-бот
- Использует minimax-m2:cloud для генерации ответов
- Поддерживает контекст разговора (последние 5 сообщений)
- Специализирован на льготах для инвалидов в России

### Голосовой ассистент
- STT (Speech-to-Text) через Mistral API
- TTS (Text-to-Speech) через Mistral API
- Работает с base64-кодированным аудио
- Поддерживает запись с микрофона в браузере

### Безопасность
- Все эндпоинты требуют авторизации
- История чата привязана к пользователю
- API ключи хранятся в .env

---

## Возможные проблемы и решения

### Ошибка: "Module 'ollama' not found"
```bash
pip install ollama
```

### Ошибка: "Module 'mistralai' not found"
```bash
pip install mistralai
```

### Ошибка: "Access to microphone denied"
- Убедитесь, что браузер имеет разрешение на доступ к микрофону
- Проверьте настройки безопасности браузера

### Ошибка при предобработке CSV
- Убедитесь, что `sfr_invalidam_data.csv` находится в корневой директории
- Проверьте OLLAMA_API_KEY в .env

### Чат-бот не отвечает
- Проверьте, что сервер запущен
- Убедитесь, что пользователь авторизован
- Проверьте OLLAMA_API_KEY в .env
- Посмотрите логи Django на наличие ошибок

---

## Что дальше?

1. Настройте промпт системы для чат-бота под ваши нужды
2. Добавьте больше категорий при необходимости
3. Настройте голосовой ассистент (выбор голоса, скорость речи)
4. Добавьте экспорт истории чата
5. Реализуйте поиск по истории чата
