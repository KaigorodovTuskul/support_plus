# Краткая инструкция по запуску

## Быстрый старт

### 1. Установка зависимостей

```bash
# Backend (только легковесные зависимости, без локальных моделей)
cd backend
pip install -r requirements.txt

# Frontend
cd ../frontend
npm install
```

**Важно**: В проекте используются **только облачные API** (Hyperbolic и Mistral), без тяжелых локальных моделей (torch, transformers и т.д.). Это делает проект легким для деплоя на VPS.

### 2. Применить миграции

```bash
cd backend
python manage.py migrate  # Миграции уже созданы
```

### 3. Предобработка CSV (опционально)

Для автоматической категоризации документов:

```bash
cd ..
python preprocess_categories.py
```

Это создаст файл `sfr_invalidam_data_categorized.csv` с категориями.

### 4. Заполнение базы данных

```bash
cd backend

# С категориями (если выполнили шаг 3)
python manage.py populate_from_csv ../sfr_invalidam_data_categorized.csv --clear

# Или без категорий
python manage.py populate_from_csv ../sfr_invalidam_data.csv --clear
```

### 5. Запуск проекта

```bash
# Backend (в одном терминале)
cd backend
python manage.py runserver

# Frontend (в другом терминале)
cd frontend
npm run dev
```

### 6. Использование чат-бота

1. Откройте http://localhost:3000
2. Войдите в систему (зарегистрируйтесь, если нужно)
3. Перейдите в Dashboard
4. Нажмите на кнопку чата в правом нижнем углу
5. Начните общение!

**Возможности чат-бота:**
- 💬 Текстовый чат
- 🎤 Голосовой ввод (нажмите на микрофон)
- 🔊 Голосовые ответы
- 📜 История сохраняется автоматически

---

## Что было реализовано

### ✅ Задача 1: Команды manage.py
- Команда `populate_from_csv.py` обновлена для поддержки категорий
- Автоматическое создание категорий из CSV

### ✅ Задача 2: Предобработка CSV
- Скрипт `preprocess_categories.py`
- Использует модель **Mistral Small** (облачно, быстро и дешево)
- Определяет категорию для каждого документа
- 11 предопределенных категорий
- Автоматическая задержка для избежания rate limits

### ✅ Задача 3: Чат-бот с голосовым ассистентом
- Новое приложение `backend/chatbot/`
- Чат-бот на **Mistral Large** (облачно)
- Голосовой ассистент через **Mistral API** (STT/TTS, облачно)
- Компонент `Chatbot.vue` на фронтенде
- Доступен после авторизации на Dashboard
- **Только один API ключ (Mistral)** - максимально просто!

---

## API Endpoints

- `POST /api/chat/` - отправить сообщение
- `GET /api/chat/history/` - получить историю
- `DELETE /api/chat/clear/` - очистить историю
- `POST /api/voice/stt/` - речь → текст
- `POST /api/voice/tts/` - текст → речь

---

## Переменные окружения (.env)

Нужен только **один ключ**:

```env
MISTRAL_API_KEY=your_mistral_api_key
```

Получить ключ: https://console.mistral.ai/api-keys/

---

Подробная документация: `IMPLEMENTATION_GUIDE.md`
